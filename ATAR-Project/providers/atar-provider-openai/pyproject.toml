[project]
name = "atar-provider-openai"
version = "0.1.0"
description = "ATAR — atar-provider-openai"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
