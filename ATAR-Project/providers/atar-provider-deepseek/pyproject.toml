[project]
name = "atar-provider-deepseek"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx", "atar-models"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atar_provider_deepseek"]
