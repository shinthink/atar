[project]
name = "atar-protocols"
version = "0.1.0"
description = "ATAR — atar-protocols"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atar_protocols"]
