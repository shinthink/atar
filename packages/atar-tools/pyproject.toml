[project]
name = "atar-tools"
version = "0.1.0"
description = "ATAR — tool runtime"
requires-python = ">=3.12"
dependencies = [
    "atar-models",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atar_tools"]
