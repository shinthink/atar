[project]
name = "atar-models"
version = "0.1.0"
description = "ATAR — atar-models"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atar_models"]
